pre-commit:
  commands:
    biome-check:
      files: "**/*.{ts,tsx}"
      run: pnpm biome check {staged_files}

pre-push:
  commands:
    biome-check:
      files: "**/*.{ts,tsx}"
      run: pnpm biome check {push_files}
